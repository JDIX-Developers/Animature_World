package protagonista;import java.awt.Image;import java.awt.event.KeyEvent;import java.awt.event.KeyListener;import javax.swing.ImageIcon;/** * Clase para controlar al protagonista * @author Jordan Arana Tejada */public class Protagonista  implements KeyListener{	double x, y;	Image imagenProta;	ImageIcon arriba = new ImageIcon("");					// Ruta de la imagen que mira hacia arriba	ImageIcon abajo= new ImageIcon("");						// Ruta de la imagen que mira hacia abajo	ImageIcon derecha = new ImageIcon("");					// Ruta de la imagen que mira hacia la derecha	ImageIcon izquierda = new ImageIcon("");				// Ruta de la imagen que mira hacia la izquierda	/**	 * Constructor para crear el protagonista.	 * @param x Las coordenadas x del protagonista.	 * @param y Las coordenadas y del protagonista.	 */	public Protagonista(double x, double y)	{			this.x = x;			this.y = y;			this.imagenProta = this.abajo.getImage();	}	/**	 * Metodo para obtener las coordenadas x del protagonista.	 * @return Las coordenadas x.	 */	public double getX()	{			return this.x;	}	/**	 * Metodo para obtener las coordenadas y del protagonista.	 * @return Las coordenadas y.	 */	public double getY()	{		return this.y;	}	/**	 * Metodo para obtener la imagen del protagonista.	 * @return La imagen del protagonista.	 */	public Image getImage()	{			return this.imagenProta;	}	/**	 * Metodo escuchador del teclado que cambia la imagen segun la tecla que se pulse.	 */	@Override	public void keyPressed(KeyEvent e)	{		int key = e.getKeyCode();		if (key == KeyEvent.VK_LEFT)		{			this.imagenProta = this.izquierda.getImage();		}		if (key == KeyEvent.VK_RIGHT)		{			this.imagenProta = this.derecha.getImage();		}		if (key == KeyEvent.VK_UP)		{			this.imagenProta = this.arriba.getImage();		}		if (key == KeyEvent.VK_DOWN)		{			this.imagenProta = this.abajo.getImage();		}	}	@Override	public void keyTyped(KeyEvent e)	{		// TODO Método autogenerado	}	@Override	public void keyReleased(KeyEvent e)	{		// TODO Método autogenerado	}}